import './App.css'
import Header from './components/Header'

import AllProjectShowCase from './components/AllProjectShowCase'

// This is the list (static data) used in the application. You can move it to any component if needed.

const App = () => (
  <>
    <Header />
    <AllProjectShowCase />
  </>
)

export default App
